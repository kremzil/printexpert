# Унификация визуального стиля проекта

## Обзор изменений

Проект приведен к единому визуальному стилю с правильной адаптацией для мобильных устройств.

## 🎨 Система контейнеров

Внедрена унифицированная система контейнеров в `app/globals.css`:

### `.container-main` 
Основной контейнер для всех страниц:
- Максимальная ширина: `1400px`
- Адаптивные отступы:
  - Mobile: `1rem` (16px)
  - Tablet (640px+): `1.5rem` (24px)
  - Desktop (1024px+): `2rem` (32px)

**Использование:**
```tsx
<div className="container-main py-6">
  {/* контент */}
</div>
```

### `.container-narrow`
Узкий контейнер для текстового контента:
- Максимальная ширина: `768px`
- Те же адаптивные отступы

**Использование:**
```tsx
<div className="container-narrow">
  <article>{/* статья */}</article>
</div>
```

### `.container-wide`
Широкий контейнер для админки и дашбордов:
- Максимальная ширина: `1920px`
- Те же адаптивные отступы

## 📱 Адаптация таблиц

### `.table-responsive`
Обертка для таблиц с горизонтальным скроллом:
- Автоматический скролл на мобильных устройствах
- Минимальная ширина таблицы: `600px` на мобильных
- Кастомные стили скроллбара

**Использование:**
```tsx
<div className="table-responsive rounded-lg border">
  <table>
    {/* содержимое таблицы */}
  </table>
</div>
```

### Обновленные компоненты
- `components/data-table.tsx` - добавлен `.table-responsive`
- `app/admin/vlastnosti/page.tsx` - таблицы с `.table-responsive`
- `app/admin/vlastnosti/[attributeId]/page.tsx` - таблицы с `.table-responsive`
- `app/admin/products/[id]/page.tsx` - матрица цен с `.table-responsive`

## 🎯 Утилиты

### Предотвращение overflow
```css
.prevent-overflow {
  overflow-x: hidden;
  max-width: 100vw;
}
```

### Обрезка текста
```css
/* Однострочное */
.text-truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Многострочное */
.line-clamp-2 { /* 2 строки */ }
.line-clamp-3 { /* 3 строки */ }
```

### Мобильные утилиты
```css
@media (max-width: 640px) {
  .mobile-compact { /* уменьшенные отступы */ }
  .mobile-hidden { /* скрыто на мобильных */ }
  .mobile-stack { /* вертикальный стек */ }
}
```

### Адаптивные сетки
```css
.card-grid {
  /* 1 колонка → 2 → 3 → 4 в зависимости от размера */
}
```

## 🔧 Исправленные проблемы

### 1. Ширина страницы
**Было:** `max-w-500` (произвольное значение)  
**Стало:** `max-w-[1400px]` (унифицированное)

**Файлы:**
- `app/layout.tsx` - главный layout
- `components/site-header-client.tsx` - header

### 2. Непостоянные отступы
**Было:** `px-12`, `px-16`, `px-4` в разных местах  
**Стало:** Единая система через `.container-main`

### 3. Проблема с w-screen
**Было:** `relative left-1/2 right-1/2 w-screen -translate-x-1/2` вызывал overflow  
**Стало:** `w-full`

**Файлы:**
- `components/print/homepage.tsx`
- `components/print/mode-selection-page.tsx`
- `components/print/empty-cart.tsx`
- `app/catalog/catalog-client.tsx`

### 4. Таблицы вылезали за экран
**Было:** `overflow-x-auto` без стилизации  
**Стало:** `.table-responsive` с адаптивным поведением

## 📐 Структура отступов

```
Mobile (< 640px):    padding: 16px
Tablet (640-1023px): padding: 24px
Desktop (≥ 1024px):  padding: 32px
```

## 🎨 Цветовая схема

Режимы B2B/B2C остались без изменений:
- **B2C**: Теплые тона (красный/оранжевый)
- **B2B**: Холодные тона (синий/индиго)

Цвета определены в CSS-переменных в `app/globals.css`.

## ✅ Результаты

- ✅ Единообразная ширина контента на всех страницах
- ✅ Адаптивные отступы для всех экранов
- ✅ Таблицы не вылезают за границы экрана
- ✅ Правильное отображение на мобильных устройствах
- ✅ Нет горизонтального скролла на странице
- ✅ Консистентный визуальный стиль
- ✅ Личный кабинет с мобильным меню
- ✅ Корзина и checkout адаптированы
- ✅ Страницы заказов унифицированы

## 📱 Мобильная навигация личного кабинета

Добавлена плавающая кнопка меню для мобильных устройств в личном кабинете:
- Кнопка отображается только на мобильных (< 1024px)
- Боковое меню открывается через Sheet
- Автоматически закрывается при переходе по ссылке
- Фиксированное положение в правом нижнем углу

## 🚀 Как использовать

### Для новых страниц

```tsx
export default function NewPage() {
  return (
    <div className="container-main py-6">
      <h1>Заголовок</h1>
      {/* контент */}
    </div>
  )
}
```

### Для таблиц

```tsx
<div className="table-responsive rounded-lg border">
  <table className="w-full">
    <thead>...</thead>
    <tbody>...</tbody>
  </table>
</div>
```

### Для админки

```tsx
<div className="container-wide py-8">
  {/* широкий контент админки */}
</div>
```

## 🔍 Тестирование

Протестируйте на следующих разрешениях:
- 📱 Mobile: 375px, 414px
- 📱 Tablet: 768px, 1024px
- 💻 Desktop: 1280px, 1440px, 1920px

## 📝 Заметки

1. Не используйте `w-screen` - это вызывает overflow
2. Всегда используйте `.container-main` вместо `container mx-auto px-4`
3. Для таблиц обязательно добавляйте `.table-responsive`
4. Header автоматически адаптируется под экран
